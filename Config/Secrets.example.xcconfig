// Copy to Secrets.xcconfig. Keep environment-specific values out of source control.
// Provider credentials belong on the server and must never be added here.
// Escape the URL slashes for xcconfig, for example: https:/$()/api.example.com/
CATALOG_PROXY_BASE_URL =
